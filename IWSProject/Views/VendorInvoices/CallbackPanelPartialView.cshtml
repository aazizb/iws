
@Html.DevExpress().CallbackPanel(settings =>
{
    settings.Name = "callbackPanelPartialView";
    settings.CallbackRouteValues = new { Controller = "VendorInvoices", Action = "CallbackPanelPartialView" };

    settings.SetContent(() =>
    {
        @Html.RenderPartial("MasterGridViewPartial");

    });
    settings.ClientSideEvents.BeginCallback = "OnBeginCallback";
}).GetHtml()
